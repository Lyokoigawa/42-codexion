mandatory arguments:

number_of_coders - number of coders + dongles

time_to_burnout (ms) - time until a coder goes into burnout.
time is reset per compile

time_to_compile - time it takes to compile. a coder must hold 2 dongles

time_to_debug - time spent debugging

time_to_refactor - time it takes to refactor. once finished,
a coder will begin searching for dongles to start compiling

number_of_compiles_required - simulation stops once this number is reached,
or a coder burns out

dongle_cooldown - after being released a dongle is
unavailable after it's been used

scheduler - the arbitation policy used by the dongles to decide who gets them.
either FIFO or EDF.
FIFO - first in, first out: dongle is granted to the coder who arrived first in the queue
EDF - earliest deadline first: deadline = last_compile_start + time_to_burnout

coder number 1 sits next to coder number_of_coders. any others sit next to N + 1 and N - 1

phases: compile -> debug -> refactor -> compile




if there's only 1 coder, only 1 dongle will be needed to compile

coders use dongles on their left and right sides, not "any" dongles like in a pile

coders will ONLY grab 2 dongles at a time



monitor controls most of the mutex stuff

have the coders sleep if they can't use dongles

when a coder finishes using the dongles, broadcast to wake everyone up

monitor will call mutexes to read if threads are finished with their bs




TO-DO:

- mutex stuff
- more thread manipulation
- controller for the simulation


2/7/2026: fixed parsing, started messing around with thread creation

27/7/2026: had to fix a buncha shit because [REDACTED] i was not happy. Disk space too low.

29/7/2026: gonna try to work on something... dunno where to go from here now.

	each coder has their own dongle on their left and the next coder's dongle on their right.
	where can i go from here now?
	adding the phases?
	yeah i think i'll try that...

	coders should check if both dongles are active before picking them up... i can do that with a
	(coder->index + 1) % config->coders
	if (dongle[coder->index]->is_active && dongle[coder->index + 1]->is_active)

31/7/2026: building monitor thread and testing compiling without monitor looking

	monitor needs these mutexes:
	- to see the status of a coder
	- 

4/8/2026: doing all the bullshit i can to make sure this shit wors... i'm so confused dude...

tempo_atual - last_use >= cooldown

7/8/2026: finally started implementing monitor control and created some more structs... now trying to implement the burnout checking.

12/8/2026:

TO-DO:
- error handling
- erase bonus prints
- 
